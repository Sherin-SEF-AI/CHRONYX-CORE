# chronyx-core is published unminified (isMinifyEnabled=false); these rules apply only if a
# consumer chooses to minify. The consumer-rules.pro is the authoritative keep set for linkers.
-keepattributes *Annotation*, Signature, InnerClasses, EnclosingMethod
